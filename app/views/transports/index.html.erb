<h1>Means of transport</h1>

<h2>You can change the kgCO2e / km of each transports if they are not correct </h2>
<div class="transports">
  <%= form_with class: "transports__form", url: "/transports", method: :post do |form| %>
    <% @transports.each do |transport| %>
      <div class="transports__container-inputs">
        <% @id_name = "id_" + transport['slug'] %>
        <%= form.label transport['slug'], transport['name'], class: "transports__label" %>
        <%= form.number_field transport['slug'], in: 0.0..999999.0, step: 0.001, value: transport['multiplier'] %>
        <%= form.hidden_field @id_name, value: transport['id'] %>
      </div>
    <% end %>
    <%= form.submit "Edit", class: "btn btn-success" %>
  <% end %>
</div>
